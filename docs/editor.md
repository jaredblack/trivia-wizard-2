# Rendering/must fix
- MC don't have letters showing

- new line getting inserted with cmd-alt-q 
- Could highlight lines that still have the default text 
- Going to have to figure out how I want to do audio. Could be acceptable to have that off-site, that’s what I’ve typically done even with GSlides 
- Need to figure out better visual cues
- answer should probably accept numbers
- remove the dum "editing live data" callout
- change editor tab title (tab titles in general all just say Trivia Wizard)
- it just had "error" at the top w/o any other indication and it just let me keep editing - bad!
- image snippet should have all the settings in there w/ defaults so you don't have to remember the names of the fields
- image syntax needs an overhaul. the image id is implicit assuming a well-behaved paste.sh
- gutter should give you a hint if the manifest has an image for that question and you don't have it in the presentation
## Routing/UI
Gave Claude 0 prompting here so no surprise it's not good right now.
- Server needs to be started to be able to get to editor at all other than by direct URL. That page could use some love

Snippet selector - should be a keyboard shortcut that brings up a snippet selector w/ different types (category, question), and then even some common different question types 
    - jk already have this